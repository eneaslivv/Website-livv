RESEND_API_KEY=re_123456789
CONTACT_EMAIL=info@livv.studio

# ─── Tracking ────────────────────────────────────────────────────────────────
# GTM is the SINGLE source of truth for analytics & ads. GA4, Google Ads,
# Meta CAPI, etc. are all configured INSIDE this container.
# Container ID is referenced from app/layout.tsx; if unset, falls back to
# the production container hardcoded in the same file.
NEXT_PUBLIC_GTM_ID=GTM-NC96QG65
